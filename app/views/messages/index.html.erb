<% if current_user %>

<%= link_to "New Message", new_message_path %>

<% end %>


<h1>Messages</h1>

<ul class="list-unstyled">
  <% @messages.each do |message| %>
  <li> <%= link_to message.to, message_path(message) %> </li>
  <% end %>
</ul>
<p><%= link_to "Return Home", root_path %></p>
